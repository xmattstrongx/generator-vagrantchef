---
driver:
  name: localhost

provisioner:
  name: chef_zero

platforms:
  - name: localhost

suites:
  - name: test
    run_list:
        - recipe[<%= name %>::create_file]
        - recipe[<%= name %>::template_file]
